# Extract the Automation_Task.rar files and import it in eclipse as Maven Project .
# Wait until Maven dependencies are downloaded and no errors are displayed on the project .
# Run the Test_XYZBank.java (TESTNG class) using TESTNG test runner 
# check the generated report under folder (Automation_Task) with Name --> emailable-report.html

# Extract the API_Task.rar and files and import it in eclipse as Maven Project .
# Wait until Maven dependencies are downloaded and no errors are displayed on the project .
# Run the Test_API_TrellaApp.java (TESTNG class) using TESTNG test runner 

